FROM nginx

ARG DOMAIN
ENV DOMAIN=$DOMAIN

ARG IPV4ADDRESS
ENV IPV4ADDRESS=$IPV4ADDRESS

ARG DOCUMENTROOT
ENV DOCUMENTROOT=$DOCUMENTROOT

ARG AUTHSERVICE_PORT
ENV AUTHSERVICE_PORT=$AUTHSERVICE_PORT



COPY ./nginx.conf /etc/nginx/conf.d/default.conf
